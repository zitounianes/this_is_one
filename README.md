# 🍔 تطبيق إدارة المطعم (Next.js Edition)

تم تحديث المشروع ليعمل بنظام Full-Stack باستخدام Next.js و Prisma و SQLite.

## ✅ ما تم إنجازه
1. **قاعدة البيانات**: تم تصميم قاعدة بيانات SQLite لحفظ الوجبات، التصنيفات، الطلبات، والإعدادات.
2. **API**: تم بناء نقاط اتصال (Endpoints) لإدارة كل البيانات ديناميكياً.
3. **الواجهة الأمامية**: تم ربط صفحات HTML/JS القديمة مع النظام الجديد.
4. **النقل**: تم نقل ملفات الواجهة إلى مجلد `public` لتعمل مع Next.js.

## 🚀 كيفية التشغيل

بما أن التثبيت (Install) قد يأخذ وقتاً، تأكد من اتباع الخطوات التالية:

1. **التثبيت (إذا لم يكتمل تلقائياً)**:
   افتح سطر الأوامر (Terminal) في مجلد المشروع واكتب:
   ```bash
   npm install
   ```

2. **تجهيز قاعدة البيانات**:
   لإنشاء قاعدة البيانات وتحديثها:
   ```bash
   npx prisma db push
   ```

3. **إضافة البيانات الأولية (Seed)**:
   لإضافة الوجبات الافتراضية لقاعدة البيانات:
   ```bash
   node prisma/seed.js
   ```

4. **تشغيل السيرفر**:
   لبدء التطبيق:
   ```bash
   npm run dev
   ```

5. **الاستخدام**:
   - افتح المتصفح على: `http://localhost:3000`
   - لوحة التحكم: `http://localhost:3000/admin.html` (كلمة السر: `admin123`)

## ⚠️ ملاحظات هامة
- قاعدة البيانات ملف محلي (`prisma/dev.db`).
- الصور المرفوعة ستكون روابط نصية حالياً (لأن رفع الملفات يحتاج إعدادات إضافية، الروابط الخارجية تعمل).

استمتع بتجربة التطبيق! 🍕
